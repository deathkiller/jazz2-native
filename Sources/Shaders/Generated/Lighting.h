// Generated by ShaderCompiler from Lighting.shader. Do not edit manually.
#pragma once

#include "ShaderCompilerTypes.h"

namespace Jazz2::ShadersGen
{
	inline constexpr char Lighting_Vs[] =
R"__SHDR__(#line 1

out vec4 vTexCoords;
out vec4 vColor;

uniform mat4 uProjectionMatrix;
uniform mat4 uViewMatrix;

layout (std140) uniform InstanceBlock
{
	mat4 modelMatrix;
	vec4 color;
	vec4 texRect;
	vec2 spriteSize;
};



uniform sampler2D uTexture;

void main()
{
	vec2 aPosition = vec2(0.5 - float(gl_VertexID >> 1), 0.5 - float(gl_VertexID % 2));
	vec4 position = vec4(aPosition.x * spriteSize.x, aPosition.y * spriteSize.y, 0.0, 1.0);

	gl_Position = uProjectionMatrix * uViewMatrix * modelMatrix * position;
	vTexCoords = texRect;
	vColor = vec4(color.x, color.y, aPosition.x * 2.0, aPosition.y * 2.0);
}
)__SHDR__";

	inline constexpr char Lighting_Vs100[] =
R"__SHDR__(attribute vec2 aQuadCorner;
#line 1

varying vec4 vTexCoords;
varying vec4 vColor;

uniform mat4 uProjectionMatrix;
uniform mat4 uViewMatrix;

	uniform mat4 modelMatrix;
	uniform vec4 color;
	uniform vec4 texRect;
	uniform vec2 spriteSize;



uniform sampler2D uTexture;

void main()
{
	vec2 aPosition = vec2(0.5 - (1.0 - aQuadCorner.x), 0.5 - aQuadCorner.y);
	vec4 position = vec4(aPosition.x * spriteSize.x, aPosition.y * spriteSize.y, 0.0, 1.0);

	gl_Position = uProjectionMatrix * uViewMatrix * modelMatrix * position;
	vTexCoords = texRect;
	vColor = vec4(color.x, color.y, aPosition.x * 2.0, aPosition.y * 2.0);
}
)__SHDR__";

	inline constexpr char Lighting_Fs[] =
R"__SHDR__(#line 1

#ifdef GL_ES
precision mediump float;
#endif

in vec4 vTexCoords;
in vec4 vColor;

uniform sampler2D uTexture;

float lightBlend(float t) {
	return t * t * t;
}

out vec4 COLOR;

void main() {
	vec2 center = vTexCoords.xy;
	float radiusNear = vTexCoords.z;
	float intensity = vColor.r;
	float brightness = vColor.g;

	float dist = length(vColor.zw);
	if (dist > 1.0) {
		COLOR = vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		// TODO: Normal maps
		/*vec4 clrNormal = texture(uTexture, vec2(gl_FragCoord) / ViewSize);
		vec3 normal = normalize(clrNormal.xyz - vec3(0.5, 0.5, 0.5));
		normal.z = -normal.z;

		vec3 lightDir = vec3((center.x - gl_FragCoord.x), (center.y - gl_FragCoord.y), 0);

		// Diffuse lighting
		float diffuseFactor = 1.0 - max(dot(normal, normalize(lightDir)), 0.0);
		diffuseFactor = diffuseFactor * 0.8 + 0.2;*/
		float diffuseFactor = 1.0;

		float strength = diffuseFactor * lightBlend(clamp(1.0 - ((dist - radiusNear) / (1.0 - radiusNear)), 0.0, 1.0));
		COLOR = vec4(strength * intensity, strength * brightness, 0.0, 1.0);
	}
}

)__SHDR__";

	inline constexpr char Lighting_Fs100[] =
R"__SHDR__(#line 1

precision mediump float;

varying vec4 vTexCoords;
varying vec4 vColor;

uniform sampler2D uTexture;

float lightBlend(float t) {
	return t * t * t;
}


void main() {
	vec4 COLOR;
	vec2 center = vTexCoords.xy;
	float radiusNear = vTexCoords.z;
	float intensity = vColor.r;
	float brightness = vColor.g;

	float dist = length(vColor.zw);
	if (dist > 1.0) {
		COLOR = vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		// TODO: Normal maps
		/*vec4 clrNormal = texture(uTexture, vec2(gl_FragCoord) / ViewSize);
		vec3 normal = normalize(clrNormal.xyz - vec3(0.5, 0.5, 0.5));
		normal.z = -normal.z;

		vec3 lightDir = vec3((center.x - gl_FragCoord.x), (center.y - gl_FragCoord.y), 0);

		// Diffuse lighting
		float diffuseFactor = 1.0 - max(dot(normal, normalize(lightDir)), 0.0);
		diffuseFactor = diffuseFactor * 0.8 + 0.2;*/
		float diffuseFactor = 1.0;

		float strength = diffuseFactor * lightBlend(clamp(1.0 - ((dist - radiusNear) / (1.0 - radiusNear)), 0.0, 1.0));
		COLOR = vec4(strength * intensity, strength * brightness, 0.0, 1.0);
	}
	gl_FragColor = COLOR;
}

)__SHDR__";

	inline constexpr ShaderCompiler::Uniform Lighting_Uniforms[] = {
		{ "uProjectionMatrix", ShaderCompiler::UniformType::Mat4, 0 },
		{ "uViewMatrix", ShaderCompiler::UniformType::Mat4, 0 },
	};

	inline constexpr ShaderCompiler::BlockMember Lighting_Block0_Members[] = {
		{ "modelMatrix", ShaderCompiler::UniformType::Mat4, 0, 0 },
		{ "color", ShaderCompiler::UniformType::Vec4, 0, 64 },
		{ "texRect", ShaderCompiler::UniformType::Vec4, 0, 80 },
		{ "spriteSize", ShaderCompiler::UniformType::Vec2, 0, 96 },
	};

	inline constexpr ShaderCompiler::UniformBlock Lighting_Blocks[] = {
		{ "InstanceBlock", 112, 0, 4, Lighting_Block0_Members },
	};

	inline constexpr ShaderCompiler::TextureBinding Lighting_Textures[] = {
		{ "uTexture", 0 },
	};

	inline constexpr ShaderCompiler::ProgramVariant Lighting_Variants[] = {
		{ "", "", Lighting_Vs, Lighting_Fs,
			2, Lighting_Uniforms, 1, Lighting_Blocks, 1, Lighting_Textures, 0, nullptr,
			Lighting_Vs100, Lighting_Fs100 },
	};

	inline constexpr ShaderCompiler::Program Lighting = { "Lighting", 0, 1, Lighting_Variants };
}
