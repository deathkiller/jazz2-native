// Generated by ShaderCompiler from LightingMeshBatch.shader. Do not edit manually.
#pragma once

#include "ShaderCompilerTypes.h"

#ifndef DOXYGEN_GENERATING_OUTPUT
namespace Jazz2::ShadersGen
{
#if defined(WITH_RHI_GL) && !defined(RHI_GL_PROFILE_ES2)
	inline constexpr char LightingMeshBatch_Vs[] =
R"__SHDR__(#line 1

in vec2 aLightCorner;
in vec2 aLightCenter;
in vec2 aLightParams;
in vec2 aLightColor;

out vec4 vTexCoords;
out vec4 vColor;

uniform mat4 uProjectionMatrix;
uniform mat4 uViewMatrix;




uniform sampler2D uTexture;

void main()
{
	vec2 worldPosition = aLightCenter + aLightCorner * aLightParams.x;
	gl_Position = uProjectionMatrix * uViewMatrix * vec4(worldPosition, 0.0, 1.0);
	vTexCoords = vec4(aLightCenter, aLightParams.y, 0.0);
	vColor = vec4(aLightColor, aLightCorner);
}
)__SHDR__";
#endif

#if defined(WITH_RHI_GL) && defined(RHI_GL_PROFILE_ES2)
	inline constexpr char LightingMeshBatch_Vs100[] =
R"__SHDR__(#line 1

attribute vec2 aLightCorner;
attribute vec2 aLightCenter;
attribute vec2 aLightParams;
attribute vec2 aLightColor;

varying vec4 vTexCoords;
varying vec4 vColor;

uniform mat4 uProjectionMatrix;
uniform mat4 uViewMatrix;




uniform sampler2D uTexture;

void main()
{
	vec2 worldPosition = aLightCenter + aLightCorner * aLightParams.x;
	gl_Position = uProjectionMatrix * uViewMatrix * vec4(worldPosition, 0.0, 1.0);
	vTexCoords = vec4(aLightCenter, aLightParams.y, 0.0);
	vColor = vec4(aLightColor, aLightCorner);
}
)__SHDR__";
#endif

#if defined(WITH_RHI_D3D11)
	inline constexpr char LightingMeshBatch_VsHlsl[] =
R"__SHDR__(// Generated HLSL (Shader Model 4/5) by ShaderCompiler. Do not edit manually.
cbuffer _Globals : register(b0)
{
	float4x4 uProjectionMatrix;
	float4x4 uViewMatrix;
};

Texture2D uTexture : register(t0);
SamplerState uTexture_sampler : register(s0);

static float2 aLightCorner;
static float2 aLightCenter;
static float2 aLightParams;
static float2 aLightColor;
static float4 gl_Position;
static float4 vTexCoords;
static float4 vColor;

struct VsInput
{
	float2 aLightCorner : TEXCOORD0;
	float2 aLightCenter : TEXCOORD1;
	float2 aLightParams : TEXCOORD2;
	float2 aLightColor : TEXCOORD3;
};

struct VsOutput
{
	float4 _clipPosition : SV_Position;
	float4 vTexCoords : TEXCOORD0;
	float4 vColor : TEXCOORD1;
};

VsOutput VSMain(VsInput _input)
{
	aLightCorner = _input.aLightCorner;
	aLightCenter = _input.aLightCenter;
	aLightParams = _input.aLightParams;
	aLightColor = _input.aLightColor;
	float2 worldPosition = aLightCenter + aLightCorner * aLightParams.x;
	gl_Position = mul(mul(uProjectionMatrix, uViewMatrix), float4(worldPosition, 0.0, 1.0));
	vTexCoords = float4(aLightCenter, aLightParams.y, 0.0);
	vColor = float4(aLightColor, aLightCorner);
	VsOutput _output = (VsOutput)0;
	_output._clipPosition = gl_Position;
	_output.vTexCoords = vTexCoords;
	_output.vColor = vColor;
	return _output;
}
)__SHDR__";
#endif

#if defined(WITH_RHI_GL) && !defined(RHI_GL_PROFILE_ES2)
	inline constexpr char LightingMeshBatch_Fs[] =
R"__SHDR__(#line 1

#ifdef GL_ES
precision mediump float;
#endif

in vec4 vTexCoords;
in vec4 vColor;

uniform sampler2D uTexture;

float lightBlend(float t) {
	return t * t * t;
}

out vec4 COLOR;

void main() {
	vec2 center = vTexCoords.xy;
	float radiusNear = vTexCoords.z;
	float intensity = vColor.r;
	float brightness = vColor.g;

	float dist = length(vColor.zw);
	if (dist > 1.0) {
		COLOR = vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		// TODO: Normal maps
		/*vec4 clrNormal = texture(uTexture, vec2(gl_FragCoord) / ViewSize);
		vec3 normal = normalize(clrNormal.xyz - vec3(0.5, 0.5, 0.5));
		normal.z = -normal.z;

		vec3 lightDir = vec3((center.x - gl_FragCoord.x), (center.y - gl_FragCoord.y), 0);

		// Diffuse lighting
		float diffuseFactor = 1.0 - max(dot(normal, normalize(lightDir)), 0.0);
		diffuseFactor = diffuseFactor * 0.8 + 0.2;*/
		float diffuseFactor = 1.0;

		float strength = diffuseFactor * lightBlend(clamp(1.0 - ((dist - radiusNear) / (1.0 - radiusNear)), 0.0, 1.0));
		COLOR = vec4(strength * intensity, strength * brightness, 0.0, 1.0);
	}
}

)__SHDR__";
#endif

#if defined(WITH_RHI_GL) && defined(RHI_GL_PROFILE_ES2)
	inline constexpr char LightingMeshBatch_Fs100[] =
R"__SHDR__(#line 1

precision mediump float;

varying vec4 vTexCoords;
varying vec4 vColor;

uniform sampler2D uTexture;

float lightBlend(float t) {
	return t * t * t;
}


void main() {
	vec4 COLOR;
	vec2 center = vTexCoords.xy;
	float radiusNear = vTexCoords.z;
	float intensity = vColor.r;
	float brightness = vColor.g;

	float dist = length(vColor.zw);
	if (dist > 1.0) {
		COLOR = vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		// TODO: Normal maps
		/*vec4 clrNormal = texture(uTexture, vec2(gl_FragCoord) / ViewSize);
		vec3 normal = normalize(clrNormal.xyz - vec3(0.5, 0.5, 0.5));
		normal.z = -normal.z;

		vec3 lightDir = vec3((center.x - gl_FragCoord.x), (center.y - gl_FragCoord.y), 0);

		// Diffuse lighting
		float diffuseFactor = 1.0 - max(dot(normal, normalize(lightDir)), 0.0);
		diffuseFactor = diffuseFactor * 0.8 + 0.2;*/
		float diffuseFactor = 1.0;

		float strength = diffuseFactor * lightBlend(clamp(1.0 - ((dist - radiusNear) / (1.0 - radiusNear)), 0.0, 1.0));
		COLOR = vec4(strength * intensity, strength * brightness, 0.0, 1.0);
	}
	gl_FragColor = COLOR;
}

)__SHDR__";
#endif

#if defined(WITH_RHI_D3D11)
	inline constexpr char LightingMeshBatch_FsHlsl[] =
R"__SHDR__(// Generated HLSL (Shader Model 4/5) by ShaderCompiler. Do not edit manually.
Texture2D uTexture : register(t0);
SamplerState uTexture_sampler : register(s0);

static float4 vTexCoords;
static float4 vColor;
static float4 COLOR;

struct PsInput
{
	float4 _fragCoord : SV_Position;
	float4 vTexCoords : TEXCOORD0;
	float4 vColor : TEXCOORD1;
};

float lightBlend(float t)
{
	return t * t * t;
}

float4 PSMain(PsInput _input) : SV_Target
{
	vTexCoords = _input.vTexCoords;
	vColor = _input.vColor;
	float2 center = vTexCoords.xy;
	float radiusNear = vTexCoords.z;
	float intensity = vColor.x;
	float brightness = vColor.y;
	float dist = length(vColor.zw);
	if (dist > 1.0) {
		COLOR = float4(0.0, 0.0, 0.0, 1.0);
	} else {
		float diffuseFactor = 1.0;
		float strength = diffuseFactor * lightBlend(clamp(1.0 - (dist - radiusNear) / (1.0 - radiusNear), 0.0, 1.0));
		COLOR = float4(strength * intensity, strength * brightness, 0.0, 1.0);
	}
	return COLOR;
}
)__SHDR__";
#endif

	inline constexpr ShaderCompiler::Uniform LightingMeshBatch_Uniforms[] = {
		{ "uProjectionMatrix", ShaderCompiler::UniformType::Mat4, 0 },
		{ "uViewMatrix", ShaderCompiler::UniformType::Mat4, 0 },
	};

	inline constexpr ShaderCompiler::TextureBinding LightingMeshBatch_Textures[] = {
		{ "uTexture", 0 },
	};

	inline constexpr ShaderCompiler::Attribute LightingMeshBatch_Attributes[] = {
		{ "aLightCorner", ShaderCompiler::UniformType::Vec2, -1 },
		{ "aLightCenter", ShaderCompiler::UniformType::Vec2, -1 },
		{ "aLightParams", ShaderCompiler::UniformType::Vec2, -1 },
		{ "aLightColor", ShaderCompiler::UniformType::Vec2, -1 },
	};

	inline constexpr ShaderCompiler::ProgramVariant LightingMeshBatch_Variants[] = {
		{ "", "",
#if defined(WITH_RHI_GL) && !defined(RHI_GL_PROFILE_ES2)
			LightingMeshBatch_Vs, LightingMeshBatch_Fs,
#else
			nullptr, nullptr,
#endif
			2, LightingMeshBatch_Uniforms, 0, nullptr, 1, LightingMeshBatch_Textures, 4, LightingMeshBatch_Attributes,
#if defined(WITH_RHI_GL) && defined(RHI_GL_PROFILE_ES2)
			LightingMeshBatch_Vs100, LightingMeshBatch_Fs100,
#else
			nullptr, nullptr,
#endif
#if defined(WITH_RHI_D3D11)
			LightingMeshBatch_VsHlsl, LightingMeshBatch_FsHlsl,
#else
			nullptr, nullptr,
#endif
#if defined(WITH_RHI_VULKAN)
			nullptr, 0, nullptr, 0 },
#else
			nullptr, 0, nullptr, 0 },
#endif
	};

	inline constexpr ShaderCompiler::Program LightingMeshBatch = { "LightingMeshBatch", 0, 1, LightingMeshBatch_Variants };
}
#endif
